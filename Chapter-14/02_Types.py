from typing import List, Tuple, Dict, Union
n : int = 5

p : str = "string"

def intReturn(a: int, c: int) -> str:
    return a + c 

intReturn()

numbers: List[int] = [1, 2, 3, 4, 5]

person: Tuple[str, int] = ("Alice", 30)

scores: Dict[str, int] = {
    "alice": 30,
    "bob": 90
    }

indentifiar: Union[int, str] = "ID123"

indentifiars = 12345 #union